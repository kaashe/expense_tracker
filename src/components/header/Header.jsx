import { Typography } from '@mui/material'
import React from 'react'

const Header = () => {
  return (
    <Typography variant="h3" textAlign={'center'}>Let's get started!</Typography>
  )
}

export default Header